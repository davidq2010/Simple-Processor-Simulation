#include "ALU.h"
#include "MUX.h"
#include "RegisterFile.h"
#include "SignExtender.h"

////////////////////////////////////////////////////////////////////////////////
/// This program construct the core circuit of the processor, using ALU, MUX, 
/// RegisterFile and SignExtender. This is proof of concept for the full
/// processor. Overall the circuit should be able to do ADD, ADDI, SUB, SLT.

/// Control lines are manully fed into the circuit, since we don't have control
/// units yet. The same for instructions, since we don't have intruction memory
////////////////////////////////////////////////////////////////////////////////
int main(int argc, char const *argv[])
{
	/* code */
	return 0;
}